# Rust
/target/
**/*.rs.bk

# Lock-файлы (если не публикуем как lib, можно добавить Cargo.lock при необходимости)
# Cargo.lock

# Cursor / служебные
.cursor/

# GitHub служебные (если нужно отслеживать — убери эту строку)
.github/

# Assets, которые не должны идти в git (шрифты, сгенерированные ресурсы и т.п.)
# Оставлено как есть, при необходимости убери, если хочешь версионировать assets
assets/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Node / web tooling (на будущее)
node_modules/
.npm
.node_repl_history
*.tgz
.yarn-integrity

# dotenv
.env

# Build artifacts (на случай фронтенда/генераторов)
dist/
build/

# Temporary
tmp/
temp/

# Backup files
*.bak