# Руководство по участию в проекте Zver

Спасибо за интерес к проекту Zver! Мы приветствуем любой вклад в развитие браузерного движка.

## 🚀 Как начать

1. **Форкните репозиторий** на GitHub
2. **Клонируйте** свой форк локально:
   ```bash
   git clone https://github.com/your-username/zver.git
   cd zver
   ```
3. **Создайте ветку** для своих изменений:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📋 Стандарты кода

### Архитектурные принципы
- **Модульность**: каждый файл должен быть меньше 300 строк
- **Разделение ответственности**: GUI код только в `zver-egui`, логика движка только в `zver`
- **Асинхронность**: используйте tokio для неблокирующих операций
- **Безопасность**: избегайте `unsafe` кода без крайней необходимости

### Стиль кода
- Используйте `cargo fmt` для форматирования
- Код должен проходить `cargo clippy -- -D warnings` без предупреждений
- Добавляйте документацию для публичных API
- Используйте осмысленные имена переменных и функций

### Структура коммитов
```
тип(область): краткое описание

Более подробное описание изменений, если необходимо.

Fixes #123
```

Типы коммитов:
- `feat`: новая функциональность
- `fix`: исправление бага
- `docs`: изменения в документации
- `style`: форматирование, отсутствующие точки с запятой и т.д.
- `refactor`: рефакторинг кода
- `test`: добавление тестов
- `chore`: обновление зависимостей, настройки сборки и т.д.

## 🧪 Тестирование

Перед отправкой PR убедитесь, что:

```bash
# Код компилируется
cargo build

# Проходят все тесты
cargo test

# Нет предупреждений clippy
cargo clippy -- -D warnings

# Код отформатирован
cargo fmt --check
```

## 📝 Добавление новой функциональности

### 1. Планирование
- Создайте issue с описанием предлагаемой функциональности
- Обсудите подход с мейнтейнерами
- Убедитесь, что функциональность вписывается в архитектуру проекта

### 2. Реализация
- Следуйте принципу "маленьких изменений"
- Добавляйте тесты для новой функциональности
- Обновляйте документацию при необходимости

### 3. Тестирование
- Протестируйте функциональность в GUI демо
- Убедитесь, что не сломали существующую функциональность
- Проверьте производительность при необходимости

## 🐛 Исправление багов

1. **Воспроизведите баг** локально
2. **Создайте тест**, который демонстрирует проблему
3. **Исправьте проблему** минимальными изменениями
4. **Убедитесь**, что тест теперь проходит
5. **Проверьте**, что не появились регрессии

## 📚 Области для вклада

### Приоритетные задачи
- **CSS поддержка**: flexbox, grid, анимации
- **JavaScript API**: больше DOM методов, события
- **Рендеринг**: тени, градиенты, трансформации
- **Производительность**: оптимизация layout и рендеринга
- **Тестирование**: unit и интеграционные тесты

### Хорошие первые задачи
- Исправление предупреждений clippy
- Добавление документации
- Написание примеров использования
- Улучшение сообщений об ошибках
- Добавление поддержки новых CSS свойств

## 🔍 Процесс ревью

1. **Создайте Pull Request** с описанием изменений
2. **Дождитесь ревью** от мейнтейнеров
3. **Внесите правки** по комментариям
4. **Дождитесь одобрения** и мержа

### Что проверяют ревьюеры
- Соответствие стандартам кода
- Архитектурная корректность
- Наличие тестов
- Производительность
- Обратная совместимость

## 📞 Связь

- **GitHub Issues**: для багов и предложений функций
- **GitHub Discussions**: для общих вопросов
- **Pull Requests**: для обсуждения кода

## 🎯 Долгосрочные цели

- Полная поддержка HTML5/CSS3
- WebAssembly интеграция
- Мобильная поддержка
- Расширяемость через плагины
- Соответствие веб-стандартам

Спасибо за ваш вклад в развитие Zver! 🚀